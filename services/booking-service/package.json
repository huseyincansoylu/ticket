{
  "name": "@ticket/booking-service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"booking-service: not implemented yet (milestone 2)\"",
    "lint": "echo \"booking-service: no lint configured yet\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:studio": "prisma studio"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "prisma": "7.10.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@prisma/client": "7.10.0",
    "dotenv": "^18.0.3"
  }
}
